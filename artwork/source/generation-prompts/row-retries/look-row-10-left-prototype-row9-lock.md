Create one NEW coherent eight-pose row-10 look strip for Codex v2 pet `pepper-carrot`.

Five references are authoritative for different purposes:

- Completed row 9 is mandatory and controls Pepper identity, rendering, hat construction, raw pose height, foot baseline, and cross-row continuity.
- The approved cardinal strip is mandatory and defines `000` up, `090` screen-right, `180` down, and `270` screen-left.
- The complete left-half prototype controls only the required row-10 left/right sequence, ordered head turns, and eight-pose spacing. Do not copy its smaller raw scale or its blink.
- The enlarged approved 180 anchor controls the open-eyed DOWN gaze.
- The enlarged approved 270 anchor controls the SCREEN-LEFT profile.

Output exactly eight complete full-body poses left to right: `180`, `202.5`, `225`, `247.5`, `270`, `292.5`, `315`, `337.5`.

SEMANTIC PROTOTYPE LOCK: preserve the prototype's complete LEFT-HALF progression. After `180`, every remaining nose/profile stays on the viewer-left side. `225` is down-left, `270` points toward the image's LEFT BORDER, `315` is up-left, and `337.5` remains subtly left while approaching up. Never reverse into the right-facing profiles shown in row 9; row 9 supplies scale and identity, not row-10 horizontal direction.

180 EYE LOCK: match the enlarged approved DOWN anchor. Keep the face broadly frontal and chin only slightly tucked. BOTH EYES are naturally open. Each green iris/pupil sits in the LOWER part of its eye aperture, touching or nearly touching the lower eyelid, with clear light sclera ABOVE and little/no sclera BELOW. It must read DOWN at 192x208, not forward, asleep, blinking, sad, or bowing. Keep 202.5 open-eyed down-left as well.

RAW REGISTRATION LOCK: generate on the same raw scale as completed row 9. On the normal 2172x724 output family, row 9 pose heights are approximately 499-509 px and the planted foot baseline is approximately y=630. Match that height range and baseline across all eight row-10 poses. Do not copy the prototype's smaller body scale. Keep every hat/profile no wider than the corresponding row-9 construction so the existing shared row-9 registration fits each final 192x208 cell without edge pixels.

Draw the entire strip together as one coherent family with the same face construction, eye size, hat width, body/head proportions, clothing, palette, lighting, line quality, scale, baseline, and registration. Express direction through eyes, face, head, and upper body; never rotate, skew, translate, mirror, or independently resize whole sprites.

Use one flat solid `#0000FF` background. Exactly eight separated connected pose groups, equal visual spacing, clear pure-blue gutters, generous outer padding, no overlap, merged groups, detached fragments, cropping, or edge contact.

Reject before returning if 180 lacks open low pupils; 270 points right; 315 is not up-left; the raw height/baseline is smaller or higher than row 9; hat width risks a final-cell edge; groups touch; or foreground reaches the source edge.

Do not add labels, degrees, numbers, arrows, clocks, grids, guide marks, shadows, scenery, extra characters, replacement eyes, or blue inside Pepper.
